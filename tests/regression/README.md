# Regression tests

Small deterministic or statistically tolerance-bounded software reference cases belong here. RAMc/published physics validation cases belong in `tests/validation/` or `benchmarks/`, never in the default coding-test suite.
